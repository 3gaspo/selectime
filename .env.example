# Prepared saved-Arrow TIME datasets and local checkpoint roots on the execution host.
TIME_DATASET=datasets/hf_dataset
TIME_WEIGHTS=weights
TIME_SEASONAL_SCOPE=shared
# Set this explicitly when invoking Python outside the scheduler runtime:
# TIME_SEASONAL_TASKS_ROOT=/absolute/path/to/seasonal/foundation_models/tasks
